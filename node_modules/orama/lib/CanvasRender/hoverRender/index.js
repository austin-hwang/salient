'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.hoverRender = undefined;

var _each2 = require('lodash/each');

var _each3 = _interopRequireDefault(_each2);

var _clearAndClip = require('../clearAndClip');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var hoverRender = exports.hoverRender = function hoverRender(props, ctx) {
  var _props$renderData = props.renderData,
      renderData = _props$renderData === undefined ? [] : _props$renderData,
      theme = props.theme;


  ctx.save();
  ctx.lineJoin = 'round';
  (0, _clearAndClip.clearAndClip)(props, ctx);

  (0, _each3.default)(renderData, function (d) {
    if (!d) return;
    if (d.type === 'line') {
      ctx.globalAlpha = d.hoverAlpha || 0.5;
      ctx.lineWidth = d.hoverLineWidth || theme.plotLineWidth;
      ctx.strokeStyle = d.hoverStroke || theme.textFill;
      ctx.stroke(d.path2D);
    } else if (d.type === 'area') {
      ctx.globalAlpha = d.hoverAlpha || 0.4;
      ctx.fillStyle = d.hoverFill || theme.textFill;
      ctx.fill(d.path2D);
    }
  });
  ctx.restore();
}; // Copyright 2017 Kensho Technologies, Inc.
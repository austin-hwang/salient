'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.clearAndClip = undefined;

var _defaults = require('../../chartCore/defaults');

var clearAndClip = exports.clearAndClip = function clearAndClip(props, ctx) {
  var width = props.width,
      height = props.height,
      _props$backgroundOffs = props.backgroundOffset,
      backgroundOffset = _props$backgroundOffs === undefined ? _defaults.BACKGROUND_OFFSET : _props$backgroundOffs;

  ctx.clearRect(0, 0, width, height);
  if (props.layerProps && props.layerProps.clipPlot === false) return;
  if (props.plotRect && props.clip) {
    ctx.beginPath();
    ctx.rect(props.plotRect.x - backgroundOffset, props.plotRect.y - backgroundOffset, props.plotRect.width + backgroundOffset * 2, props.plotRect.height + backgroundOffset * 2);
    ctx.clip();
  }
}; // Copyright 2017 Kensho Technologies, Inc.
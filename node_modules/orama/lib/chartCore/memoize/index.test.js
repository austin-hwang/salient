'use strict';

var _mocha = require('mocha');

var _assert = require('assert');

var _assert2 = _interopRequireDefault(_assert);

var _ = require('./');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

(0, _mocha.it)('Chart/memoize.getMemoizeDimArrays', function () {
  var memoizeAddDimArrays = (0, _.getMemoizeDimArrays)();
  var props = {
    localAccessors: { x: 'x' },
    data: [{ x: 1 }, { x: 2 }, { x: 3 }],
    x: 'x'
  };
  var result = memoizeAddDimArrays(props);
  var expected = memoizeAddDimArrays(props).xArray;
  var actual = result.xArray;
  _assert2.default.equal(expected, actual);
}); // Copyright 2017 Kensho Technologies, Inc.
'use strict';

var _mocha = require('mocha');

var _assert = require('assert');

var _assert2 = _interopRequireDefault(_assert);

var _ = require('./');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

(0, _mocha.it)('Chart/getForProps 1', function () {
  var getFunc = function getFunc() {
    return 'test';
  };
  var getTest = (0, _.getForProps)('Test', getFunc);
  var props = {
    groupedKeys: ['x', 'y']
  };
  var actual = getTest(props);
  var expected = {
    xTest: 'test',
    yTest: 'test'
  };
  _assert2.default.deepEqual(actual, expected);
}); // Copyright 2017 Kensho Technologies, Inc.

(0, _mocha.it)('Chart/getForProps 2', function () {
  var getFunc = function getFunc() {
    return 'test';
  };
  var getTest = (0, _.getForProps)('Test', getFunc);
  var props = {
    groupedKeys: ['x', 'y'],
    xTest: 'overrided'
  };
  var actual = getTest(props);
  var expected = {
    yTest: 'test'
  };
  _assert2.default.deepEqual(actual, expected);
});
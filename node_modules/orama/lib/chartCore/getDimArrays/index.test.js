'use strict';

var _mocha = require('mocha');

var _assert = require('assert');

var _assert2 = _interopRequireDefault(_assert);

var _ = require('./');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// Copyright 2017 Kensho Technologies, Inc.

(0, _mocha.it)('Chart/getDimArrays.wrapUpNewProps 1', function () {
  var dimArrays = {
    x: [1, 2],
    y: ['a', 'b']
  };
  var actual = (0, _.wrapUpNewProps)(dimArrays);
  var expected = {
    xArray: [1, 2],
    yArray: ['a', 'b'],
    groupedKeys: ['x', 'y']
  };
  _assert2.default.deepEqual(actual, expected);
});

(0, _mocha.it)('Chart/getDimArrays.getDimArrays 1', function () {
  var layer1 = {
    data: [{ x: 1 }, { x: 2 }],
    localAccessors: { x: 'x' }
  };
  var layer2 = {
    data: [{ x: 3 }, { x: 4 }],
    localAccessors: { x: 'x' }
  };
  var props = {
    layers: [layer1, layer2]
  };
  var actual = (0, _.getDimArrays)(props);
  var expected = {
    xArray: [1, 2, 3, 4],
    groupedKeys: ['x']
  };
  _assert2.default.deepEqual(actual, expected);
});
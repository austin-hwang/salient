'use strict';

var _mocha = require('mocha');

var _assert = require('assert');

var _assert2 = _interopRequireDefault(_assert);

var _ = require('./');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

(0, _mocha.it)('TooltipWrapper.getTooltipPosition 1', function () {
  _assert2.default.deepEqual((0, _.getTooltipPosition)({}), {});
}); // Copyright 2017 Kensho Technologies, Inc.

(0, _mocha.it)('TooltipWrapper.getTooltipPosition 2', function () {
  var props = {
    width: 400, height: 400,
    mouse: { x: 0, y: 0 }
  };
  var result = (0, _.getTooltipPosition)(props);
  _assert2.default.deepEqual(result, { left: 0, top: 0 });
});
(0, _mocha.it)('TooltipWrapper.getTooltipPosition 3', function () {
  var props = {
    width: 400, height: 400,
    mouse: { x: 900, y: 0 }
  };
  var result = (0, _.getTooltipPosition)(props);
  _assert2.default.deepEqual(result, { right: 100, top: 0 });
});
(0, _mocha.it)('TooltipWrapper.getTooltipPosition 4', function () {
  var props = {
    width: 400, height: 400,
    mouse: { x: 0, y: 900 }
  };
  var result = (0, _.getTooltipPosition)(props);
  _assert2.default.deepEqual(result, { bottom: 100, left: 0 });
});